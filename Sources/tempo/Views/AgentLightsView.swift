import SwiftUI

// Placeholder — replaced with state-colored agent session lights (decision 005).
struct AgentLightsView: View {
    var sessions: [AgentSession]
    var body: some View { EmptyView() }
}
