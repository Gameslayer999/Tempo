import SwiftUI

// Placeholder — replaced with playlist list + add-current-track (decision 003).
struct PlaylistSection: View {
    @ObservedObject var api: SpotifyWebAPI
    @ObservedObject var state: AppState
    var body: some View { EmptyView() }
}
